import 'package:flutter/material.dart';

class AppColors {
  // Primary Blue
  static const Color primary = Color(0xFF008CFF);

  // Text colors
  static const Color textDark = Color(0xFF000000);
  static const Color textLight = Color(0xFF777777);

  // Background
  static const Color background = Colors.white;

  // Light blue inputs (مثل التصميم)
  static const Color inputBackground = Color(0xFFEAF3FF);

  // Gradient colors للأزرار
  static const Color gradientStart = Color(0xFF0094FF);
  static const Color gradientEnd = Color(0xFF006BFF);
}

